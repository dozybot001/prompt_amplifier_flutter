name: prompt_amplifier
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # 图标库
  cupertino_icons: ^1.0.8

  # 核心修复：降级到 2.x 的最终版本，完美支持 StateNotifier
  flutter_riverpod: ^2.6.1

  # 路由：配合 Riverpod 2.x，使用 14.x 版本最稳定
  go_router: ^14.0.0

  # 动画库
  flutter_animate: ^4.5.0

  # 本地存储
  flutter_secure_storage: ^9.0.0

  # 网络请求
  dio: ^5.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true